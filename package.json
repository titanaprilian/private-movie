{
  "name": "monorepo-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14 <2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "turbo run start",
    "dev": "turbo run dev",
    "dev:backend": "turbo run dev --filter=@repo/backend",
    "dev:web": "turbo run dev --filter=@repo/web",
    "test": "turbo run test",
    "test:android-tv": "turbo run test --filter=@repo/android-tv",
    "test:integration": "turbo run test:integration",
    "db:generate": "cd packages/db && bun run db:generate",
    "db:push": "cd packages/db && bun run db:push",
    "db:migrate": "cd packages/db && bun run db:migrate",
    "db:seed": "cd packages/db && bun run db:seed",
    "db:sync-episodes": "cd packages/db && bun run db:sync-episodes",
    "db:fill-tmdb-ids": "cd packages/seed-cli && bun run db:fill-tmdb-ids",
    "fill-tmdb-ids": "cd packages/seed-cli && bun run fill-tmdb-ids",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5",
    "vitest": "^3.1.0"
  }
}
